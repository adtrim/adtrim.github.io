# GitHub Pages publishes the repo root directly via Actions — no build folder.
# Keeping this ignore in case a local preview tool ever writes one.
public/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# AI tool scaffolding
.claude/
CLAUDE.md
